<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jakting.shareclean">
    <queries>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>
    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SettingsActivity"
            android:label="@string/setting_title"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".MiscActivity"
            android:label="@string/misc_title"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".AboutActivity"
            android:theme="@style/AppTheme.About" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SendManageActivity"
            android:label="@string/send_manage_card_title"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".ViewManageActivity"
            android:label="@string/view_manage_card_title"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>